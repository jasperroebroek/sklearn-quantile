[build-system]
requires = ["setuptools>=61.2", "cython>=3", "numpy"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]

[project]
name = "sklearn-quantile"
description = "README.md"
dynamic = ["version"]
